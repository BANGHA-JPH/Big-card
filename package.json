{
  "name": "zita-vegetables-ecommerce",
  "version": "1.0.0",
  "description": "Full-stack e-commerce application for selling vegetables",
  "scripts": {
    "install:all": "npm install && cd mobile && npm install && cd ../backend && npm install",
    "start:mobile": "cd mobile && npm start",
    "start:backend": "cd backend && npm run dev",
    "start": "concurrently \"npm run start:backend\" \"npm run start:mobile\"",
    "build:mobile": "cd mobile && npm run build",
    "build:backend": "cd backend && npm run build",
    "test": "npm run test:mobile && npm run test:backend",
    "test:mobile": "cd mobile && npm test",
    "test:backend": "cd backend && npm test",
    "lint": "npm run lint:mobile && npm run lint:backend",
    "lint:mobile": "cd mobile && npm run lint",
    "lint:backend": "cd backend && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "private": true,
  "workspaces": [
    "mobile",
    "backend"
  ]
}
